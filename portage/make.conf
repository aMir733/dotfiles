# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
COMMON_FLAGS="-O2 -pipe"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"

# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C

USE="a52 aac aalib acl alsa amd64 appindicator berkdb bluetooth bzip2 cairo cjk cli crypt curl dbus dhcpcd dri dts dvd elogind encode expat ffmpeg flac fontconfig fortran ftp gdbm gif git gles2 gtk gtk3 iconv icu ipv6 jpeg json libglvnd libtirpc libv4l luajit matroska mp3 mp4 mpeg mount multilib ncurses network networkmanager nls nptl ogg opencl opengl openh264 openmp openssl pam pcre png policykit posix pulseaudio python readline screencast seccomp session sockets sound split-usr sqlite ssh ssl svg syslog tcpd threads tiff tray truetype udev udisks unicode usb user-session v4l vaapi vdpau vorbis vulkan wayland x264 x265 xattr xml xmpp xvid zink zip zlib zsh-completion -nvidia -systemd"
MAKEOPTS="-j11"
#FEATURES="keepwork"
ACCEPT_LICENSE="*"
#ACCEPT_KEYWORDS="~amd64"
GENTOO_MIRRORS="https://mirror.bytemark.co.uk/gentoo/"
GRUB_PLATFORMS="efi-64"
