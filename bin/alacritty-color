#!/bin/bash

[[ -f $1 ]] || exit 1
ALACRITTY_CONF="$HOME/.config/alacritty/alacritty.yml"
cp $ALACRITTY_CONF{,.backup}
sed -i '/^# Colors \(.*\)/,$ d' $ALACRITTY_CONF || exit 1
cat "$1" | sed "s/background: .*/background: '0x191919'/g" >> $ALACRITTY_CONF
